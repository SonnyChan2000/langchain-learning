# AI 智能助手 CLI（AI Assistant CLI）

基于大模型 **Function Calling（函数调用）** 实现的命令行智能助手。模型自动理解用户意图并决定是否调用工具、调用哪个工具、调用几次；Python 代码负责真正执行函数并把结果回传，模型再基于结果生成自然语言回答。

支持多轮对话上下文记忆、连续工具调用，内置天气查询、数学计算、时间查询三个工具。

---

## 功能特性

- **多轮对话**：维护完整对话历史，助手记得上下文（如先问"北京天气"，再问"要带伞吗"能接上）
- **工具自动选择**：无需手动指定，模型根据问题自动选择工具（问天气 → 天气工具，问计算 → 计算器）
- **连续工具调用**：一次提问可触发多轮工具调用（如"北京和上海哪个热"会连续查询两个城市）
- **三个内置工具**：
  - `get_weather(city)`：城市天气查询（当前为模拟数据，可替换为真实天气 API）
  - `calculate(a, b, operator)`：精确数学计算（加减乘除）
  - `get_current_time()`：当前日期、时间、星期查询（无参数工具示例）
- **对话管理命令**：输入 `clear` 清空历史重新开始，输入 `退出` 结束程序

---

## 工作原理

Function Calling 的核心分工：**模型负责决策，代码负责执行**。

```
用户提问
   │
   ▼
┌──────────────────────────────────┐
│  外层循环：多轮对话                │
│  input 接收输入 → 维护 messages    │
└───────────────┬──────────────────┘
                │
                ▼
┌──────────────────────────────────┐
│  内层循环：Agent 工具调用闭环       │
│                                  │
│  ① 请求模型（messages + tools）    │
│        │                         │
│   ┌────┴─────────┐               │
│   ▼              ▼               │
│ 模型返回 tool_calls   模型直接回答 │
│   │              │               │
│   ▼              │               │
│ ② 执行 Python 函数 │              │
│ ③ 结果作为 tool 消息回传          │
│   │              │               │
│   └──► 回到 ① 再请求   打印答案，break
└──────────────────────────────────┘
                │
                ▼
        回到外层循环，等待下一句
```

**关键设计：**

1. **tools 工具说明书**：用 JSON Schema 向模型描述每个函数的名称、功能、参数。模型靠 `description` 判断何时该用哪个工具
2. **双层循环架构**：外层管理对话状态，内层处理"模型可能连续 N 次调用工具才肯回答"的情况
3. **四种消息角色**：`system`（助手设定）、`user`（用户输入）、`assistant`（模型回复，含工具调用指令）、`tool`（函数执行结果）
4. **安全实践**：API Key 通过 `.env` 文件管理，不写入代码

---

## 快速开始

### 环境要求

- Python 3.10+
- 智谱 AI API Key（GLM-4-Flash 模型免费，注册即得：https://open.bigmodel.cn/ ）

### 安装

```bash
pip install openai python-dotenv
```

### 配置

在项目根目录创建 `.env` 文件：

```
ZHIPU_API_KEY=你的API密钥
```

> 确保 `.gitignore` 中包含 `.env`，避免密钥泄露。

### 运行

```bash
python day18_assistant.py
```

---

## 使用示例

```
========================================
智能助手已启动！
输入 clear 清空对话历史，输入 退出 结束
========================================

你：现在几点了？
[系统] 正在调用 get_current_time，参数 {} ...
AI：现在是 2026年9月2日 14:20，星期二。

你：杭州天气怎么样？
[系统] 正在调用 get_weather，参数 {'city': '杭州'} ...
AI：杭州今天晴天，气温 28 度，天气不错。

你：3827乘以941等于多少？
[系统] 正在调用 calculate，参数 {'a': 3827, 'b': 941, 'operator': '*'} ...
AI：3827 × 941 = 3601207。

你：北京和上海哪个温度高？
[系统] 正在调用 get_weather，参数 {'city': '北京'} ...
[系统] 正在调用 get_weather，参数 {'city': '上海'} ...
AI：北京 22 度，上海 26 度，上海更热。

你：那我出门要带伞吗？
AI：北京有小雨，建议带伞；上海多云不用。
```

---

## 技术栈

| 技术 | 说明 |
|------|------|
| Python | 主语言 |
| OpenAI SDK | 调用大模型（兼容 OpenAI 协议，切换平台只需改 base_url） |
| 智谱 GLM-4-Flash | 大模型（免费） |
| python-dotenv | 环境变量管理 |
| Function Calling | 工具调用机制 |

---

## 可扩展方向

- 将 `get_weather` 内部替换为真实天气 API（如和风天气、高德天气）
- 增加更多工具：网页搜索、文件读写、数据库查询、邮件发送
- 接入流式输出（stream=True）实现打字机效果
- 异常重试：网络抖动时自动重试 API 请求
- 用 FastAPI 封装为 Web 服务，提供 HTTP 接口

## Day25：扣子（Coze）低代码平台 · 个人知识助手

在扣子开发平台搭建知识库问答 Agent「个人知识助手」，不写代码复刻手写 RAG 全链路：

- 创建智能体并编写人设与回复逻辑（含"资料外问题禁止编造"约束）
- 创建文本知识库，导入个人学习笔记，平台自动完成切片、向量化、建索引
- 挂载知识库并开启自动调用（混合检索 + 召回配置）
- 验收测试：库内问题准确引用资料（如 RAG 五步流程、FastAPI 启动命令、笔记独有实测数据），库外问题（天气、React）按约束拒答，无幻觉
- 已发布上线扣子商店，可在线体验：https://www.coze.cn/store/agent/7683400604764487721?bot_id=true

## Day26: 扣子(Coze)对话流·智能客服

第二个平台型项目：用扣子可视化工作流搭建电商智能客服，覆盖"意图识别 → 条件分流 → 知识库检索 → 回复生成"的完整客服链路。

- **架构**：一个大模型节点做意图识别，只输出 after_sale / order / chat 结构化标签；选择器节点按标签做三分路分发（售后 / 订单 / 闲聊）；售后分支接知识库检索（平台版 RAG）生成退货指引；三条路经收口节点统一对外回复
- **实践亮点 — 知识库"召回时灵时不灵"排查**：逐层定位到自动分段过粗——整个文档被切成两大块、向量不聚焦，相似度在阈值边晃动；改按章节分段后稳定召回。沉淀为"分段粒度决定召回质量"的 RAG 工程经验
- **实践亮点 — 多分支收口取空值**：结束节点单绑定一个分支，其它路跑时就取不到值返回空；加收口节点让三条路共用统一出口，彻底解决
- **实践亮点 — TopK 多条只读第一条**：检索返回 TopK 多条时绑定整个列表喂给大模型，必要时用代码节点拼成规整文本，避免模型只看第一条
- **验收**：接入智能体后端到端测三类问题全通过——耳机退货（正确返回退换货/退款规则）、今天天气不错（自然承接闲聊并引导）、查订单到哪了（引导提供订单号），已发布扣子商店
- **体验链接**：https://www.coze.cn/s/Njg7-Cq8DBg/

**双路径实践**：与手写 ReAct / Function Calling Agent 形成对照——同样的"意图识别 + 分流 + 检索 + 生成"思想，既用代码范式亲手实现过，也能在可视化工作流上零代码落地，并能讲清两种方式的取舍（确定流程用工作流固化、灵活决策用 Agent）。同时沉淀了工作流 vs 对话流在对话类应用上的选型经验。

